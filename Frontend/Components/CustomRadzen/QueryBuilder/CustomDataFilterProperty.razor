@using Frontend.Components.CustomRadzen.QueryBuilder.Models
@typeparam TItem
@implements IDisposable

@code {
    // This component does not render any content directly.
    // It's used to define properties that will be rendered by the CustomDataFilter component.
}